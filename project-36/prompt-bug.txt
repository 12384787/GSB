任务类型：Bug 修复
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：strapi/strapi（TypeScript）。项目原本是：🚀 Strapi is the leading open-source headless CMS. It’s 100% JavaScript/TypeScript, fully customizable, and developer-first.。当前源码入口可从 .ai, .coderabbit.yaml, .commitlintrc.ts, .cursor, .editorconfig, .gitattributes, .github, .gitignore, .husky, .npmrc, .nvmrc, .nxignore 进入。
Strapi 在 content-type schema 变更、草稿发布和 webhook 重试同时发生时，API 返回的字段结构与管理后台不一致。请从 core、admin、database、webhook 和测试构造迁移失败/重复事件，修复 schema 缓存、事务边界和权限过滤；保持插件与 REST/GraphQL 兼容。
实现以最小影响面为准，保留现有协议、错误类型和日志语义；用故障注入证明失败路径会收敛、资源会释放、重试不会放大副作用，最后跑一遍项目原有测试集。
