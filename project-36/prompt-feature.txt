任务类型：Feature 迭代
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：strapi/strapi（TypeScript）。项目原本是：🚀 Strapi is the leading open-source headless CMS. It’s 100% JavaScript/TypeScript, fully customizable, and developer-first.。当前源码入口可从 .ai, .coderabbit.yaml, .commitlintrc.ts, .cursor, .editorconfig, .gitattributes, .github, .gitignore, .husky, .npmrc, .nvmrc, .nxignore 进入。
给 Strapi 增加可审计的内容发布流水线，支持草稿校验、审批、定时发布、回滚和 webhook 投递状态。实现需贯通 content types、admin、database、RBAC、REST/GraphQL 和插件生命周期，处理 schema 迁移、幂等、部分失败、权限变更和旧项目升级，补齐集成测试与文档。
先验证现有行为再扩展，新增能力要有降级和迁移方案。验收不仅看成功结果，还要检查权限、取消、恢复、性能和观测信息；把关键取舍写在变更说明里，确保后续维护者能复现问题和回滚。
