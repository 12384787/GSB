任务类型：Bug 修复
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：pallets/flask（Python）。项目原本是：The Python micro framework for building web applications.。当前源码入口可从 .devcontainer, .editorconfig, .github, .gitignore, .pre-commit-config.yaml, .readthedocs.yaml, CHANGES.rst, LICENSE.txt, README.md, docs, examples, pyproject.toml 进入。
Flask 的 async view 在请求超时或客户端断开后，后台 task 仍可能写 response 或持有 request context，导致连接泄漏和偶发 RuntimeError。请从 src/flask、上下文管理、测试 client 和文档示例构造可重复并发用例，修复取消传播、上下文生命周期和错误转换；同步验证 WSGI、ASGI 适配及旧扩展。
请把修复前后的行为差异写进回归用例，尤其是乱序、重复、取消和旧输入；禁止删除断言、改掉公共契约或把并发问题粗暴改成全局串行。提交说明要给出根因和未覆盖风险。
