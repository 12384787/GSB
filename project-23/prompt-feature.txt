任务类型：Feature 迭代
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：pallets/flask（Python）。项目原本是：The Python micro framework for building web applications.。当前源码入口可从 .devcontainer, .editorconfig, .github, .gitignore, .pre-commit-config.yaml, .readthedocs.yaml, CHANGES.rst, LICENSE.txt, README.md, docs, examples, pyproject.toml 进入。
给 Flask 增加一个面向 ASGI 部署的可配置请求生命周期钩子，让扩展能安全感知开始、取消、完成和异常，并兼容现有 WSGI 信号。需要定义 async/sync 混用、嵌套请求、contextvars、错误处理、超时和扩展版本迁移，补齐 docs、examples、测试客户端和兼容性测试。
这项迭代必须能在真实部署里收敛。请明确状态机、失败重试和幂等边界，补上接口/类型/文档变更以及至少一条端到端路径；不能通过复制一套平行实现、硬编码样例或牺牲旧 API 来完成。
