任务类型：Feature 迭代
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：microsoft/markitdown（多语言）。项目原本是：。当前源码入口可从 .devcontainer, .dockerignore, .gitattributes, .github, .gitignore, .pre-commit-config.yaml, CODE_OF_CONDUCT.md, Dockerfile, LICENSE, README.md, SECURITY.md, SUPPORT.md 进入。
给 MarkItDown 增加可组合的批量转换流水线，支持按文件类型选择转换器、并行度、失败继续/失败即停、结构化元数据和结果清单。要处理插件优先级、编码、附件资源、超大文件、取消、重复输入和旧 CLI 参数，补齐跨格式夹具、性能与安全测试。
不要只交一个 happy path：把旧配置、升级中断、并发调用、部分成功和重复触发都纳入设计。实现要落在现有模块边界内，给出可检查的测试、日志/指标和回滚方式，并说明新增存储或协议如何兼容历史数据。
