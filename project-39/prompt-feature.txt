任务类型：Feature 迭代
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：coder/coder（Go）。项目原本是：Secure environments for developers and their agents。当前源码入口可从 .agents, .claude, .cursorrules, .devcontainer, .dockerignore, .editorconfig, .git-blame-ignore-revs, .gitattributes, .github, .gitignore, .golangci.yaml, .markdownlint-cli2.jsonc 进入。
给 Coder 增加可恢复的 workspace 生命周期编排，支持创建、休眠、唤醒、升级、失败回滚和 agent 重新注册。要求 server、provisioner、agent、权限和审计共享版本化状态，处理并发操作、云端部分成功、凭据轮换、配额和旧 workspace 迁移，补齐故障注入与 API/CLI 测试。
这项迭代必须能在真实部署里收敛。请明确状态机、失败重试和幂等边界，补上接口/类型/文档变更以及至少一条端到端路径；不能通过复制一套平行实现、硬编码样例或牺牲旧 API 来完成。
