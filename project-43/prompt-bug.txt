任务类型：Bug 修复
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：langgenius/dify（多语言）。项目原本是：。当前源码入口可从 .agents, .claude, .coveragerc, .devcontainer, .dockerignore, .editorconfig, .gitattributes, .github, .gitignore, .vite-hooks, .vscode, AGENTS.md 进入。
Dify 的 workflow 在工具调用超时后重试，变量状态、额度扣减和流式输出可能各自推进一次，导致下一节点拿到不完整上下文。请从 backend、workflow、tool、queue 和 web 构造流中断/重复 webhook/并发会话，修复节点幂等与状态快照；验收旧 DSL、权限、secret 和人工停止。
请把修复前后的行为差异写进回归用例，尤其是乱序、重复、取消和旧输入；禁止删除断言、改掉公共契约或把并发问题粗暴改成全局串行。提交说明要给出根因和未覆盖风险。
