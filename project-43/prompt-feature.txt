任务类型：Feature 迭代
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：langgenius/dify（多语言）。项目原本是：。当前源码入口可从 .agents, .claude, .coveragerc, .devcontainer, .dockerignore, .editorconfig, .gitattributes, .github, .gitignore, .vite-hooks, .vscode, AGENTS.md 进入。
给 Dify 增加可恢复的长流程检查点与人工接管机制，允许管理员查看节点输入输出、暂停、修改批准后的变量并从安全边界继续。需要贯通 workflow runtime、queue、tool、模型调用、配额、审计和 web，处理版本迁移、敏感数据、重复执行与部分失败，补齐集成/权限测试。
这项迭代必须能在真实部署里收敛。请明确状态机、失败重试和幂等边界，补上接口/类型/文档变更以及至少一条端到端路径；不能通过复制一套平行实现、硬编码样例或牺牲旧 API 来完成。
