任务类型：Bug 修复
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：github/spec-kit（Python）。项目原本是：💫 Toolkit to help you get started with Spec-Driven Development。当前源码入口可从 .devcontainer, .editorconfig, .gitattributes, .github, .gitignore, .markdownlint-cli2.jsonc, .pre-commit-config.yaml, .specify, .zenodo.json, AGENTS.md, CHANGELOG.md, CITATION.cff 进入。
Spec Kit 在模板版本升级、spec/plan/tasks 生成和扩展命令并发执行时，可能把不同 feature 的文件写到同一目录，最终状态看似成功但无法继续实现。请从 src、templates、scripts、extensions 和 tests 构造重复命名、半写入、恢复执行场景，修复锁、事务式写入、版本校验和错误退出；保持现有 CLI 与旧模板兼容。
先把失败压缩成一个稳定夹具，再改实现；不要用重试、睡眠或禁用校验把问题藏起来。最终要能从测试输出看出状态、资源和错误传播都恢复正常，并说明为什么不会回归到相邻模块。
