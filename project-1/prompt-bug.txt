任务类型：Bug 修复
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：grafana/loki（Go）。项目原本是：Like Prometheus, but for logs.。当前源码入口可从 .claude, .cursor, .devcontainer, .dockerignore, .gitattributes, .github, .gitignore, .gitmodules, .golangci.yml, .lychee.toml, .release-please-manifest.json, .vscode 进入。
Loki 查询在 querier、frontend 和 cache 之间重试时出现重复或丢失日志，尤其是带时间范围和 tail 语义的请求。请从 pkg、cmd、integration 的调用链构造能稳定复现的并发测试，查清请求拆分、缓存键、上下文取消和错误合并哪个环节破坏了结果；修复后要证明超时重试不会重复计数、取消能释放 goroutine，并保留现有 LogQL 和 HTTP 响应语义。
先补一个修复前会失败的测试，再定位根因并做最小改动；不要删测试、放宽断言、硬编码结果或顺手重构无关模块。验收时同时跑原有回归、异常路径、并发/取消边界和兼容性检查，并在说明里写清触发条件、根因、修复影响面。
