任务类型：Bug 修复
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：itchyny/gojq（Go）。项目原本是：Pure Go implementation of jq。当前源码入口可从 .dockerignore, .gitattributes, .github, .gitignore, CHANGELOG.md, Dockerfile, LICENSE, Makefile, README.md, _gojq, _tools, builtin.go 进入。
gojq 在流式输入遇到深层嵌套 JSON、无效 UTF-8 和管道下游提前退出时，可能吞掉错误或继续读取导致进程无法结束。请从 parser、iterator、CLI 和 tests 构造组合用例，定位 token 缓冲、stdin 关闭和退出码传播问题；修复后验证与 jq 的兼容输出和资源释放。
先补一个修复前会失败的测试，再定位根因并做最小改动；不要删测试、放宽断言、硬编码结果或顺手重构无关模块。验收时同时跑原有回归、异常路径、并发/取消边界和兼容性检查，并在说明里写清触发条件、根因、修复影响面。
