任务类型：Bug 修复
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：vuejs/vue（多语言）。项目原本是：。当前源码入口可从 .editorconfig, .git-blame-ignore-revs, .github, .gitignore, .prettierrc, BACKERS.md, CHANGELOG.md, LICENSE, README.md, api-extractor.json, api-extractor.tsconfig.json, benchmarks 进入。
Vue 在 keep-alive、异步组件和 SSR hydration 同时更新时，旧 vnode 的 effect scope 没有正确停止，导致重复 watch 或客户端 DOM 错配。请从 runtime-core、runtime-dom、server-renderer 和测试构造嵌套条件，修复生命周期/缓存失效顺序；验收 dev/prod、teleport 和旧组件选项 API。
请把修复前后的行为差异写进回归用例，尤其是乱序、重复、取消和旧输入；禁止删除断言、改掉公共契约或把并发问题粗暴改成全局串行。提交说明要给出根因和未覆盖风险。
