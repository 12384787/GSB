任务类型：Bug 修复
任务难度：困难
语言/框架：Python

请在已有的 rotki/rotki 项目中排查并修复一个影响生产使用的跨模块缺陷。项目定位为：A portfolio tracking, analytics, accounting and management application that protects your privacy。围绕真实入口（.bumpversion.cfg,.codecov.yml .coveragerc,.dockerignore .gitattributes,.github .gitignore,.ignore .pylint.rc,.readthedocs.yaml AGENTS.md,CLAUDE.md CONTRIBUTING.md,Cargo.lock Cargo.toml,Dockerfile LICENSE.md,Makefile README.md,_typos.toml colibri,crates docs,frontend funding.json,package.py packaging,pyproject.toml releases_sponsors.md,rotkehlchen.spec rotkehlchen,rotkehlchen_mock stubs,tools uv.lock）对应的核心链路，复现一个状态不一致、错误传播、并发边界、资源生命周期或兼容性问题；先记录触发步骤和根因，再做最小范围修复，不能绕过逻辑、删除测试或改变无关 API。补充覆盖正常、失败、边界和回归场景的测试，并验证错误信息、退出码或响应语义仍可诊断。
