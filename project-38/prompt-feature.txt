任务类型：Feature 迭代
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：rotki/rotki（Python）。项目原本是：A portfolio tracking, analytics, accounting and management application that protects your privacy。当前源码入口可从 .bumpversion.cfg, .codecov.yml, .coveragerc, .dockerignore, .gitattributes, .github, .gitignore, .ignore, .pylint.rc, .readthedocs.yaml, AGENTS.md, CLAUDE.md 进入。
给 Rotki 增加可解释的组合估值快照与差异审计，用户能看到每个资产、价格来源、交易影响和同步时间，并对异常值选择重算或忽略。要贯通 exchanges、blockchain、accounting、cache 和 UI，处理离线、精度、重复交易、隐私和旧数据库迁移，补齐确定性夹具测试。
不要只交一个 happy path：把旧配置、升级中断、并发调用、部分成功和重复触发都纳入设计。实现要落在现有模块边界内，给出可检查的测试、日志/指标和回滚方式，并说明新增存储或协议如何兼容历史数据。
