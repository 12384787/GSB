任务类型：Feature 迭代
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：openxla/xla（C++）。项目原本是：A machine learning compiler for GPUs, CPUs, and ML accelerators。当前源码入口可从 .bant-macros, .bazelrc, .bazelversion, .clang-format, .clang-tidy, .clangd, .github, .gitignore, .kokoro, .pre-commit-config.yaml, AUTHORS, BUILD.bazel 进入。
给 XLA 增加带版本与设备约束的编译缓存审计和失效机制，允许按模块查看命中原因、编译耗时、内存占用并安全清理。要求贯通 HLO pipeline、runtime、service、各后端和并发编译，处理动态 shape、驱动变化、失败回退、序列化兼容与旧缓存迁移，补齐跨后端测试和 benchmark。
先画出现有调用链和数据边界，再按项目惯用方式落地；新能力必须有失败回滚、重复执行、权限/兼容性和可观测性设计，不能用全局开关掩盖错误。补齐单元、集成或端到端测试、文档与迁移说明，并证明旧用例、新用例和异常恢复都能稳定通过。
