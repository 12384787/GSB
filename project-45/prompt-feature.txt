任务类型：Feature 迭代
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：open-webui/open-webui（多语言）。项目原本是：。当前源码入口可从 .dockerignore, .env.example, .eslintignore, .eslintrc.cjs, .gitattributes, .github, .gitignore, .npmrc, .pre-commit-config.yaml, .prettierignore, .prettierrc, CHANGELOG.md 进入。
给 Open WebUI 增加可追溯的对话运行记录，关联模型版本、检索文档、工具调用、token 用量和用户反馈，并支持从失败节点重试而不复制消息。需要贯通 backend、RAG、工具、前端流式状态、权限和导出，处理隐私脱敏、并发、旧记录迁移和大对话性能，补齐集成测试。
先画出现有调用链和数据边界，再按项目惯用方式落地；新能力必须有失败回滚、重复执行、权限/兼容性和可观测性设计，不能用全局开关掩盖错误。补齐单元、集成或端到端测试、文档与迁移说明，并证明旧用例、新用例和异常恢复都能稳定通过。
