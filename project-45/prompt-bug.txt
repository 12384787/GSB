任务类型：Bug 修复
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：open-webui/open-webui（多语言）。项目原本是：。当前源码入口可从 .dockerignore, .env.example, .eslintignore, .eslintrc.cjs, .gitattributes, .github, .gitignore, .npmrc, .pre-commit-config.yaml, .prettierignore, .prettierrc, CHANGELOG.md 进入。
Open WebUI 在模型切换、知识库检索和聊天流式响应并发时，旧请求的 token 会插入新会话，检索引用也与回答不匹配。请从 backend、retrieval、web socket/stream 和 frontend 状态构造断线、重试、取消用例，修复会话代次、引用绑定和权限过滤；验收多用户与旧模型接口。
先补一个修复前会失败的测试，再定位根因并做最小改动；不要删测试、放宽断言、硬编码结果或顺手重构无关模块。验收时同时跑原有回归、异常路径、并发/取消边界和兼容性检查，并在说明里写清触发条件、根因、修复影响面。
