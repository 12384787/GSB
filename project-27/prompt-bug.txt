任务类型：Bug 修复
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：ultralytics/ultralytics（Python）。项目原本是：Ultralytics YOLO27, YOLO26, YOLO11, YOLOv8 — object detection, instance segmentation, semantic segmentation, image classification, pose estimation, object tracking。当前源码入口可从 .dockerignore, .github, .gitignore, AGENTS.md, CITATION.cff, CLAUDE.md, CONTRIBUTING.md, LICENSE, README.md, README.zh-CN.md, docker, docs 进入。
Ultralytics 的训练/推理流水线在多 GPU、断点恢复和验证集自动下载同时启用时，checkpoint 可能记录错误的 epoch 或设备，恢复后指标与日志错位。请从 engine、datasets、nn 和 tests 构造中断/重试场景，修复设备映射、随机种子、文件原子写入和 CLI 兼容；不能用删除旧 checkpoint 规避。
请把修复前后的行为差异写进回归用例，尤其是乱序、重复、取消和旧输入；禁止删除断言、改掉公共契约或把并发问题粗暴改成全局串行。提交说明要给出根因和未覆盖风险。
