任务类型：Bug 修复
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：Checkmk/checkmk（Python）。项目原本是：Checkmk。当前源码入口可从 .aspect, .bazelignore, .bazeliskrc, .bazelrc, .bazelversion, .buildifier.json, .cargo, .clang-tidy, .clangd, .claude, .clippy.toml, .dockerignore 进入。
Checkmk 的 agent 数据采集、规则评估和 inventory 更新在一个主机超时后，可能沿用上一次服务状态并错误触发告警恢复。请从 Python check engine、agent bakery、inventory 和 Web API 复现超时/重试/并发刷新，修复状态新鲜度、缓存键和告警去抖；不能改变插件协议。
先把失败压缩成一个稳定夹具，再改实现；不要用重试、睡眠或禁用校验把问题藏起来。最终要能从测试输出看出状态、资源和错误传播都恢复正常，并说明为什么不会回归到相邻模块。
