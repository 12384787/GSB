任务类型：Bug 修复
任务难度：困难
语言/框架：Python

请在已有的 Checkmk/checkmk 项目中排查并修复一个影响生产使用的跨模块缺陷。项目定位为：Checkmk - Best-in-class infrastructure & application monitoring。围绕真实入口（.aspect,.bazelignore .bazeliskrc,.bazelrc .bazelversion,.buildifier.json .cargo,.clang-tidy .clangd,.claude .clippy.toml,.dockerignore .editorconfig,.envrc .git-blame-ignore-revs,.gitattributes .github,.gitignore .gitmodules,.ide .iwyu_mappings,.mcp.json .npmrc,.paseo .pre-commit-config.yaml,.pre-commit-scripts .prettierignore,.shellcheckrc .site,.stylelintignore .stylelintrc.mjs,.werks .yamllint.yml,AGENTS.md AUTHORS,BAZEL.md BUILD,CHANGES CLAUDE.md,CONTRIBUTING.md COPYING,Cargo.lock Cargo.toml,MODULE.bazel MODULE.bazel.lock,Makefile PSScriptAnalyzerSettings.psd1,README.md SECURITY.md,TESTING）对应的核心链路，复现一个状态不一致、错误传播、并发边界、资源生命周期或兼容性问题；先记录触发步骤和根因，再做最小范围修复，不能绕过逻辑、删除测试或改变无关 API。补充覆盖正常、失败、边界和回归场景的测试，并验证错误信息、退出码或响应语义仍可诊断。
