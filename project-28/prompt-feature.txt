任务类型：Feature 迭代
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：KittyCAD/modeling-app（TypeScript）。项目原本是：The Zoo Design Studio app.。当前源码入口可从 .env.development, .env.production, .envrc, .gitattributes, .github, .gitignore, .helix, .husky, .npmrc, .nvmrc, .prettierignore, .tool-versions 进入。
给 Modeling App 增加可恢复的参数化建模历史时间线，用户能查看每次操作、依赖、耗时和错误，并从任意节点分支或回滚。实现要协调编辑器状态、几何服务、项目存储、权限和离线缓存，处理并发操作、版本迁移、二进制资产和失败重放，补齐端到端测试。
先验证现有行为再扩展，新增能力要有降级和迁移方案。验收不仅看成功结果，还要检查权限、取消、恢复、性能和观测信息；把关键取舍写在变更说明里，确保后续维护者能复现问题和回滚。
