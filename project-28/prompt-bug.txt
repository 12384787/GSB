任务类型：Bug 修复
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：KittyCAD/modeling-app（TypeScript）。项目原本是：The Zoo Design Studio app.。当前源码入口可从 .env.development, .env.production, .envrc, .gitattributes, .github, .gitignore, .helix, .husky, .npmrc, .nvmrc, .prettierignore, .tool-versions 进入。
KittyCAD modeling-app 在参数编辑触发异步建模、撤销重做和选择集更新同时发生时，视图显示的几何体与 feature tree 不一致。请从 src、server/client API、建模状态和测试复现乱序响应，修复操作代次、取消、错误回滚和持久化；验收大模型、断线重连和旧项目加载。
实现以最小影响面为准，保留现有协议、错误类型和日志语义；用故障注入证明失败路径会收敛、资源会释放、重试不会放大副作用，最后跑一遍项目原有测试集。
