任务类型：Feature 迭代
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：meshery/meshery（TypeScript）。项目原本是：Meshery, the cloud native manager。当前源码入口可从 .agents, .claude, .dockerignore, .gitattributes, .github, .gitignore, .gitpod.yml, ADOPTERS.md, AGENTS.md, CHANGELOG.md, CLAUDE.md, CNAME 进入。
给 Meshery 增加跨 provider 的模型发布编排：用户可以校验依赖、分阶段发布、观察进度并在失败时回滚。要求 server、mesheryctl、models、provider-ui 和 ui 共享版本化状态，处理权限、幂等、部分成功、provider 能力差异和历史配置迁移，补齐集成测试、审计事件与降级方案。
这项迭代必须能在真实部署里收敛。请明确状态机、失败重试和幂等边界，补上接口/类型/文档变更以及至少一条端到端路径；不能通过复制一套平行实现、硬编码样例或牺牲旧 API 来完成。
