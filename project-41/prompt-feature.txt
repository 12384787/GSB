任务类型：Feature 迭代
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：ollama/ollama（多语言）。项目原本是：。当前源码入口可从 .dockerignore, .gitattributes, .github, .gitignore, .golangci.yaml, AGENTS.md, CLAUDE.md, CMakeLists.txt, CMakePresets.json, CONTRIBUTING.md, Dockerfile, LICENSE 进入。
给 Ollama 增加可恢复的模型分层缓存与拉取任务 API，支持断点续传、校验、并发去重、磁盘配额、模型淘汰和推理中的引用保护。要求 server、runner、storage、CLI 与 OpenAI 兼容 API 协同，处理权限/凭据、失败重试、取消和旧 manifest 迁移，补齐集成与压力测试。
先画出现有调用链和数据边界，再按项目惯用方式落地；新能力必须有失败回滚、重复执行、权限/兼容性和可观测性设计，不能用全局开关掩盖错误。补齐单元、集成或端到端测试、文档与迁移说明，并证明旧用例、新用例和异常恢复都能稳定通过。
