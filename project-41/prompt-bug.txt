任务类型：Bug 修复
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：ollama/ollama（多语言）。项目原本是：。当前源码入口可从 .dockerignore, .gitattributes, .github, .gitignore, .golangci.yaml, AGENTS.md, CLAUDE.md, CMakeLists.txt, CMakePresets.json, CONTRIBUTING.md, Dockerfile, LICENSE 进入。
Ollama 在模型拉取、并发推理和客户端断开同时发生时，模型 blob 可能被提前清理，另一个请求收到截断输出。请从 server、runner、storage、API 和测试注入下载失败/取消/重启，修复引用计数、流关闭和错误传播；验收多模型并发和旧 API。
先补一个修复前会失败的测试，再定位根因并做最小改动；不要删测试、放宽断言、硬编码结果或顺手重构无关模块。验收时同时跑原有回归、异常路径、并发/取消边界和兼容性检查，并在说明里写清触发条件、根因、修复影响面。
