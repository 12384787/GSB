任务类型：Bug 修复
任务难度：困难
语言/框架：多语言

请在已有的 ollama/ollama 项目中排查并修复一个影响生产使用的跨模块缺陷。项目定位为：。围绕真实入口（.dockerignore,.gitattributes .github,.gitignore .golangci.yaml,AGENTS.md CLAUDE.md,CMakeLists.txt CMakePresets.json,CONTRIBUTING.md Dockerfile,LICENSE LLAMA_CPP_VERSION,MLX_C_VERSION MLX_VERSION,README.md SECURITY.md,anthropic api,app auth,cmake cmd,create discover,docs envconfig,format fs,go.mod go.sum,harmony integration,internal llama,llm logutil,main.go manifest,middleware ml,mlx mlxrunner,model openai,parser progress,readline scripts,server template,thinking tools,types version,x）对应的核心链路，复现一个状态不一致、错误传播、并发边界、资源生命周期或兼容性问题；先记录触发步骤和根因，再做最小范围修复，不能绕过逻辑、删除测试或改变无关 API。补充覆盖正常、失败、边界和回归场景的测试，并验证错误信息、退出码或响应语义仍可诊断。
