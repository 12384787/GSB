任务类型：Bug 修复
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：iamkun/dayjs（JavaScript）。项目原本是：⏰ Day.js 2kB immutable date-time library alternative to Moment.js with the same modern API。当前源码入口可从 .editorconfig, .eslintrc.json, .github, .gitignore, .npmignore, .releaserc, CHANGELOG.md, CONTRIBUTING.md, LICENSE, README.md, babel.config.js, build 进入。
Day.js 在 timezone、utc、locale 插件叠加后，跨 DST 边界的 startOf/endOf 与 format 结果依赖插件加载顺序，链式对象还可能被意外共享。请从 src、plugin、types 和 test 构造多个时区/年份/locale 组合，修复不可变性、偏移计算和类型声明；保持 Moment 兼容 API。
实现以最小影响面为准，保留现有协议、错误类型和日志语义；用故障注入证明失败路径会收敛、资源会释放、重试不会放大副作用，最后跑一遍项目原有测试集。
