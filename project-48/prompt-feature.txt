任务类型：Feature 迭代
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：iamkun/dayjs（JavaScript）。项目原本是：⏰ Day.js 2kB immutable date-time library alternative to Moment.js with the same modern API。当前源码入口可从 .editorconfig, .eslintrc.json, .github, .gitignore, .npmignore, .releaserc, CHANGELOG.md, CONTRIBUTING.md, LICENSE, README.md, babel.config.js, build 进入。
给 Day.js 增加可插拔的时区数据加载与缓存策略，支持按需加载 IANA 区域、DST 规则查询和无数据时的明确降级。要求兼容 utc/timezone/locale 插件、不可变链式 API、浏览器体积目标和 TypeScript 类型，处理缓存版本、并发加载、历史日期和旧构建产物，补齐跨时区测试。
先验证现有行为再扩展，新增能力要有降级和迁移方案。验收不仅看成功结果，还要检查权限、取消、恢复、性能和观测信息；把关键取舍写在变更说明里，确保后续维护者能复现问题和回滚。
