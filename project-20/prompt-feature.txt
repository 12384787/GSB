任务类型：Feature 迭代
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：mui/mui-x（TypeScript）。项目原本是：MUI X: Build complex and data-rich applications using a growing list of advanced React components, like the Data Grid, Date and Time Pickers, Charts, and more!。当前源码入口可从 .agents, .browserslistrc, .circleci, .claude, .editorconfig, .gitattributes, .github, .gitignore, .lintignore, .remarkrc.mjs, .vale.ini, .vscode 进入。
为 MUI X Data Grid 增加可组合的异步分组聚合能力，支持服务端分页、虚拟化、树数据、筛选排序和实时更新一起使用。要求定义缓存键、请求取消、错误恢复、选择/编辑语义和 Pro/Premium 类型兼容，补齐性能、键盘可访问性、RTL 和回归测试。
先验证现有行为再扩展，新增能力要有降级和迁移方案。验收不仅看成功结果，还要检查权限、取消、恢复、性能和观测信息；把关键取舍写在变更说明里，确保后续维护者能复现问题和回滚。
