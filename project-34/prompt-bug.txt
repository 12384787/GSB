任务类型：Bug 修复
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：koajs/koa（JavaScript）。项目原本是：Expressive middleware for node.js using ES2017 async functions。当前源码入口可从 .codecov.yml, .editorconfig, .github, .gitignore, .mailmap, AUTHORS, CODE_OF_CONDUCT.md, History.md, LICENSE, Readme.md, __tests__, docs 进入。
Koa 的 middleware 在 response 已经 commit 后又抛出异常，onerror、流关闭和上下文状态更新顺序不一致，某些请求会挂住。请从 lib/application、compose、context 和 __tests__ 构造异步嵌套、客户端断开、流式响应和超时场景，修复错误边界与资源释放；不能破坏 koa-compose 语义。
先把失败压缩成一个稳定夹具，再改实现；不要用重试、睡眠或禁用校验把问题藏起来。最终要能从测试输出看出状态、资源和错误传播都恢复正常，并说明为什么不会回归到相邻模块。
