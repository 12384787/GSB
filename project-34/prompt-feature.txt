任务类型：Feature 迭代
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：koajs/koa（JavaScript）。项目原本是：Expressive middleware for node.js using ES2017 async functions。当前源码入口可从 .codecov.yml, .editorconfig, .github, .gitignore, .mailmap, AUTHORS, CODE_OF_CONDUCT.md, History.md, LICENSE, Readme.md, __tests__, docs 进入。
给 Koa 增加基于 AbortSignal 的请求取消和超时协作机制，要求 middleware、stream、context 和错误处理能区分客户端断开、服务端超时与业务取消。处理嵌套 signal、已发送响应、代理头、旧 middleware 和 Node 版本兼容，补齐测试、类型声明、文档和性能基线。
不要只交一个 happy path：把旧配置、升级中断、并发调用、部分成功和重复触发都纳入设计。实现要落在现有模块边界内，给出可检查的测试、日志/指标和回滚方式，并说明新增存储或协议如何兼容历史数据。
