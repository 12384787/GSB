任务类型：Bug 修复
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：handsontable/handsontable（JavaScript）。项目原本是：JavaScript Data Grid / Data Table with a Spreadsheet Look & Feel. Works with React, Angular, and Vue. Supported by the Handsontable team ⚡。当前源码入口可从 .ai, .changelogs, .claude, .cursor, .editorconfig, .eslintignore, .eslintrc.js, .gitattributes, .github, .gitignore, .mcp.json, .npmrc 进入。
Handsontable 在合并单元格、批量粘贴、撤销重做和异步校验叠加时，视图值与 data source 不一致。请从 core、plugins、React/Vue wrappers 和测试复现一次跨插件事务被拆开的情况，修复变更批次、selection 和 renderer 刷新顺序；验收大数据量和旧插件接口。
先补一个修复前会失败的测试，再定位根因并做最小改动；不要删测试、放宽断言、硬编码结果或顺手重构无关模块。验收时同时跑原有回归、异常路径、并发/取消边界和兼容性检查，并在说明里写清触发条件、根因、修复影响面。
