任务类型：Bug 修复
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：twentyhq/twenty（TypeScript）。项目原本是：The open alternative to Salesforce, designed for AI.。当前源码入口可从 .claude, .dockerignore, .gitattributes, .github, .gitignore, .mcp.json, .nvmrc, .oxfmtrc.jsonc, .vscode, .yarn, .yarnrc.yml, AGENTS.md 进入。
Twenty 的 CRM 活动流在联系人合并、实时订阅和分页加载同时发生时，前端会出现重复卡片或把新活动插入错误位置。请从 server、UI 和数据同步模块复现排序游标失效的场景，检查事务边界、事件去重和权限过滤；修复必须覆盖刷新、断线重连、软删除和旧客户端。
请把修复前后的行为差异写进回归用例，尤其是乱序、重复、取消和旧输入；禁止删除断言、改掉公共契约或把并发问题粗暴改成全局串行。提交说明要给出根因和未覆盖风险。
