任务类型：Bug 修复
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：langflow-ai/langflow（多语言）。项目原本是：。当前源码入口可从 .agents, .coderabbit.yaml, .composio.lock, .cursor, .devcontainer, .dockerignore, .env.example, .eslintrc.json, .gitattributes, .github, .gitignore, .pre-commit-config.yaml 进入。
Langflow 在组件 schema 热更新、图执行并发和流式输出取消同时发生时，旧节点配置会被新 schema 错误解释，执行结果还可能写错 run。请从 backend、custom components、execution、frontend 和 tests 复现，修复版本绑定、取消传播和持久化；保留已有 flow JSON。
实现以最小影响面为准，保留现有协议、错误类型和日志语义；用故障注入证明失败路径会收敛、资源会释放、重试不会放大副作用，最后跑一遍项目原有测试集。
