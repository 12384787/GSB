任务类型：Feature 迭代
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：langflow-ai/langflow（多语言）。项目原本是：。当前源码入口可从 .agents, .coderabbit.yaml, .composio.lock, .cursor, .devcontainer, .dockerignore, .env.example, .eslintrc.json, .gitattributes, .github, .gitignore, .pre-commit-config.yaml 进入。
给 Langflow 增加带版本锁定的 flow 发布与回滚能力，支持组件依赖检查、环境变量校验、灰度运行和执行记录对比。要协调 graph schema、custom components、execution、API、前端画布和存储，处理并发编辑、旧组件、secret、失败回滚与长任务恢复，补齐端到端测试。
先验证现有行为再扩展，新增能力要有降级和迁移方案。验收不仅看成功结果，还要检查权限、取消、恢复、性能和观测信息；把关键取舍写在变更说明里，确保后续维护者能复现问题和回滚。
