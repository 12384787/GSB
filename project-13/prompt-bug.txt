任务类型：Bug 修复
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：archestra-ai/archestra（TypeScript）。项目原本是：Enterprise AI Platform with guardrails, MCP registry, gateway & orchestrator。当前源码入口可从 .agents, .claude, .codex, .github, .gitignore, AGENTS.md, CLAUDE.md, EXTERNAL_CONTRIBUTORS.md, LICENSE.md, LICENSE_AGPL, LICENSE_ENTERPRISE, README.md 进入。
Archestra 的 MCP gateway 在工具注册更新、请求路由和 guardrail 配置热加载同时发生时，可能把请求发给已撤销的工具。请从 registry、gateway、orchestrator 和测试构造权限版本错配，检查缓存、并发读写和错误回退；修复要覆盖流式响应中断、租户隔离、重试及旧配置。
先补一个修复前会失败的测试，再定位根因并做最小改动；不要删测试、放宽断言、硬编码结果或顺手重构无关模块。验收时同时跑原有回归、异常路径、并发/取消边界和兼容性检查，并在说明里写清触发条件、根因、修复影响面。
