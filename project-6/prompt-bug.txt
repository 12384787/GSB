任务类型：Bug 修复
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：AtalayaLabs/OxiCloud（Rust）。项目原本是：☁️ Ultra-fast, secure & lightweight self-hosted cloud storage — your files, photos, calendars & contacts, all in one place. Built in Rust.。当前源码入口可从 .cargo, .claudeignore, .dockerignore, .envrc, .gitattributes, .github, .gitignore, .stylelintrc.json, AGENTS.md, CLAUDE.md, CODEOWNERS, CODE_OF_CONDUCT.md 进入。
OxiCloud 在目录递归扫描、断点下载和文件删除同时发生时，返回的 ETag 与实际内容不匹配，客户端会缓存旧文件。请在 Rust 服务、索引和 HTTP 层构造并发测试，定位元数据提交与文件句柄生命周期的竞态；修复后验证 Range、If-None-Match、软删除恢复和断点续传，不能靠禁用缓存解决。
先把失败压缩成一个稳定夹具，再改实现；不要用重试、睡眠或禁用校验把问题藏起来。最终要能从测试输出看出状态、资源和错误传播都恢复正常，并说明为什么不会回归到相邻模块。
