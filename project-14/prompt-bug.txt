任务类型：Bug 修复
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：openobserve/openobserve（TypeScript）。项目原本是：Open source observability platform for logs, metrics, traces, RUM, Session replay, pipelines, SLO and LLM observability. A sophisticated, simple and highly performant alternative to Datadog, Splunk, and Elasticsearch with 140x lower storage costs and single binary deployment.。当前源码入口可从 .agents, .cargo, .claude, .claudeignore, .cursorignore, .devcontainer, .env.example, .gitattributes, .github, .gitignore, .typos.toml, CLAUDE.md 进入。
OpenObserve 的日志摄取、管道转换和查询索引在高峰期背压时，可能出现已确认但未索引的数据，查询结果与 ingestion offset 对不上。请从 server、pipeline、web 和存储层注入磁盘满、worker 重启、重复批次和 schema 变化，定位确认点与索引提交的竞态；修复需保持多租户隔离和旧数据可读。
先把失败压缩成一个稳定夹具，再改实现；不要用重试、睡眠或禁用校验把问题藏起来。最终要能从测试输出看出状态、资源和错误传播都恢复正常，并说明为什么不会回归到相邻模块。
