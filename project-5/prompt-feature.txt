任务类型：Feature 迭代
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：Kuingsmile/PicList（Vue）。项目原本是：An image upload and manage tool, base on PicGo。当前源码入口可从 .cz-config.cjs, .editorconfig, .gitattributes, .github, .gitignore, .husky, .prettierrc, .stylelintignore, .stylelintrc.cjs, .vscode, .winget, CHANGELOG.md 进入。
为 PicList 增加可恢复的批量上传任务面板，要求插件、队列、Vue 状态和本地持久化协同支持暂停/继续、并发上限、过期凭证刷新、失败项重试和重复文件去重。要兼容现有 PicGo 插件协议，记录每个文件的最终远端 URL 与错误原因，并补齐端到端测试和迁移路径。
先画出现有调用链和数据边界，再按项目惯用方式落地；新能力必须有失败回滚、重复执行、权限/兼容性和可观测性设计，不能用全局开关掩盖错误。补齐单元、集成或端到端测试、文档与迁移说明，并证明旧用例、新用例和异常恢复都能稳定通过。
