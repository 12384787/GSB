任务类型：Bug 修复
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：metabase/metabase（Clojure）。项目原本是：The easy-to-use open source Business Intelligence and Embedded Analytics tool that lets everyone work with data :bar_chart:。当前源码入口可从 .babel_cache, .claude, .clj-kondo, .clj-surgeon.edn, .cljfmt.edn, .coderabbit.yaml, .codespellignore, .codespellrc, .dependency-cruiser.cjs, .devcontainer, .dir-locals.el, .dockerignore 进入。
Metabase 的查询任务在数据库连接超时后进入重试，但任务状态、查询缓存和审计记录可能分别显示失败、运行中和成功。请围绕查询执行器、队列、缓存和 API 复现一次真实状态分裂，定位事务与异步回调的竞态；修复要覆盖用户取消、权限撤销、重复 webhook 和进程重启，不能简单关闭重试。
先把失败压缩成一个稳定夹具，再改实现；不要用重试、睡眠或禁用校验把问题藏起来。最终要能从测试输出看出状态、资源和错误传播都恢复正常，并说明为什么不会回归到相邻模块。
