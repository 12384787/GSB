任务类型：Bug 修复
任务难度：地狱

帮我直接按现有代码查这件事，先别急着改实现：
仓库：sanity-io/sanity（TypeScript）。项目原本是：Sanity Studio – Rapidly configure content workspaces powered by structured content。当前源码入口可从 .agents, .claude, .cursor, .editorconfig, .env.example, .envrc, .github, .gitignore, .oxfmtrc.json, .oxlintrc.json, .vscode, AGENTS.md 进入。
Sanity Studio 在 schema 热更新、表单 patch 合并和实时协作事件同时到达时，字段值会短暂回滚，随后保存错误的旧版本。请从 studio、form、desk/tool 和测试复现乱序 patch 与断线重连，修复版本判断、草稿/发布边界和校验错误传播；要保留插件 API。
先补一个修复前会失败的测试，再定位根因并做最小改动；不要删测试、放宽断言、硬编码结果或顺手重构无关模块。验收时同时跑原有回归、异常路径、并发/取消边界和兼容性检查，并在说明里写清触发条件、根因、修复影响面。
