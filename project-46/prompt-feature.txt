任务类型：Feature 迭代
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：react/react（多语言）。项目原本是：。当前源码入口可从 .claude, .codesandbox, .editorconfig, .eslintignore, .eslintrc.js, .git-blame-ignore-revs, .gitattributes, .github, .gitignore, .mailmap, .nvmrc, .prettierignore 进入。
给 React 增加一套面向外部异步资源的稳定缓存/订阅原语，要求在 concurrent、Suspense、Server Components 和 SSR hydration 下保持一致快照。需要定义取消、错误边界、预加载、缓存失效、开发诊断和旧 API 互操作，补齐 reconciler fixtures、浏览器/Node 测试及性能基线。
不要只交一个 happy path：把旧配置、升级中断、并发调用、部分成功和重复触发都纳入设计。实现要落在现有模块边界内，给出可检查的测试、日志/指标和回滚方式，并说明新增存储或协议如何兼容历史数据。
