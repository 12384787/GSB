任务类型：Bug 修复
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：react/react（多语言）。项目原本是：。当前源码入口可从 .claude, .codesandbox, .editorconfig, .eslintignore, .eslintrc.js, .git-blame-ignore-revs, .gitattributes, .github, .gitignore, .mailmap, .nvmrc, .prettierignore 进入。
React 的 concurrent rendering、外部 store 更新和 Suspense 边界组合时，组件可能在旧 render 中触发副作用，表现为重复订阅或 hydration 不一致。请从 reconciler、scheduler、react-dom 和现有 fixtures 构造最小复现，定位提交/回滚与 effect 清理顺序；修复必须保持 legacy mode、SSR 和开发警告语义。
先把失败压缩成一个稳定夹具，再改实现；不要用重试、睡眠或禁用校验把问题藏起来。最终要能从测试输出看出状态、资源和错误传播都恢复正常，并说明为什么不会回归到相邻模块。
