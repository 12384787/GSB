任务类型：Bug 修复
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：getsentry/sentry-javascript（TypeScript）。项目原本是：Official Sentry SDKs for JavaScript。当前源码入口可从 .agents, .claude, .craft.yml, .cursor, .editorconfig, .git-blame-ignore-revs, .github, .gitignore, .madgerc, .oxfmtrc.json, .oxlintrc.base.json, .oxlintrc.json 进入。
sentry-javascript 在多个 SDK 集成、source map 上传和 offline transport 同时启用时，事件可能被重复上报或把用户上下文串到下一个请求。请从 packages、transport、scope 和测试复现异步 flush、页面卸载、重试和 worker 场景，修复 scope 生命周期与队列幂等；不能牺牲现有浏览器兼容。
先把失败压缩成一个稳定夹具，再改实现；不要用重试、睡眠或禁用校验把问题藏起来。最终要能从测试输出看出状态、资源和错误传播都恢复正常，并说明为什么不会回归到相邻模块。
