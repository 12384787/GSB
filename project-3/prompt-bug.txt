任务类型：Bug 修复
任务难度：地狱

请接着当前仓库的实现往下排查，不要另起一套架构：
仓库：Expensify/App（TypeScript）。项目原本是：Welcome to New Expensify: a complete re-imagination of financial collaboration, centered around chat. Help us build the next generation of Expensify by sharing feedback and contributing to the code.。当前源码入口可从 .buckconfig, .bun-version, .bundle, .claude, .env.example, .env.production, .env.staging, .gitattributes, .github, .gitignore, .gitmodules, .imgbotconfig 进入。
Expensify/App 的聊天式报销流程在离线编辑、网络恢复和多设备同步同时发生时，报告状态与本地草稿会互相覆盖。请从 src、server、modules、tests 的真实同步链路复现一个能稳定触发的冲突，定位队列顺序、乐观更新或重放逻辑的根因；修复时要覆盖附件上传失败、重复请求、权限变化和旧缓存升级，不能只改 UI 或放宽断言。
请把修复前后的行为差异写进回归用例，尤其是乱序、重复、取消和旧输入；禁止删除断言、改掉公共契约或把并发问题粗暴改成全局串行。提交说明要给出根因和未覆盖风险。
