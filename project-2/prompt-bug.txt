任务类型：Bug 修复
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：langbot-app/LangBot（Python）。项目原本是：Production-grade platform for building agentic IM bots。当前源码入口可从 .dockerignore, .github, .gitignore, .mcp.json, .pre-commit-config.yaml, AGENTS.md, ARCHITECTURE.md, CLA.md, CLAUDE.md, CONTRIBUTING.md, Dockerfile, LICENSE 进入。
LangBot 在多平台事件进入 Agent、插件和知识库链路时，重试可能让同一条消息被回复两次，失败事件又无法准确恢复。请围绕 src、skills、web 和 tests 找到真实入口，模拟并发 webhook、流式输出中断、插件超时和会话切换，修复幂等键、状态落库与异常传播之间的不一致；必须保留 Discord、飞书等适配器的现有行为。
先把失败压缩成一个稳定夹具，再改实现；不要用重试、睡眠或禁用校验把问题藏起来。最终要能从测试输出看出状态、资源和错误传播都恢复正常，并说明为什么不会回归到相邻模块。
