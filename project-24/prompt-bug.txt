任务类型：Bug 修复
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：encode/httpx（Python）。项目原本是：A next generation HTTP client for Python. 🦋。当前源码入口可从 .github, .gitignore, CHANGELOG.md, LICENSE.md, README.md, docs, httpx, mkdocs.yml, pyproject.toml, requirements.txt, scripts, tests 进入。
HTTPX 在 HTTP/2 多路复用连接遇到单个 stream 重置时，连接池可能把失效连接继续分配给其他请求，表现为随机 ReadError 或请求悬挂。请从 httpx client、transports、pool、async 关闭逻辑和 tests 注入 reset/timeout/取消组合，修复连接健康状态与错误重试边界；不能破坏 HTTP/1.1 行为。
实现以最小影响面为准，保留现有协议、错误类型和日志语义；用故障注入证明失败路径会收敛、资源会释放、重试不会放大副作用，最后跑一遍项目原有测试集。
