任务类型：Bug 修复
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：xberg-io/xberg（Rust）。项目原本是：Polyglot document intelligence with a Rust core: extract text, metadata, images, tables, and structured data from 106 formats across 140 file extensions, plus code intelligence for 371 languages. Fifteen bindings, with CLI, REST API, and MCP server.。当前源码入口可从 .agents, .ai-rulez-generated.json, .ai-rulez, .alef-generation.toml, .alef-ownership.toml, .alef-toml-merge-provenance.toml, .cargo, .clang-format, .claude-plugin, .dockerignore, .editorconfig, .gh-actions-updater.toml 进入。
Xberg 在 PDF/OCR 文档解析中途取消时，Rust core 已经写入部分图片和表格结果，Python/Node 调用方却收到成功响应。请沿 extractor、FFI、队列和测试夹具复现取消、损坏输入和多线程调用，修复资源释放、错误映射和临时文件清理；必须保持 ABI 与已有输出格式。
实现以最小影响面为准，保留现有协议、错误类型和日志语义；用故障注入证明失败路径会收敛、资源会释放、重试不会放大副作用，最后跑一遍项目原有测试集。
