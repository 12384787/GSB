任务类型：Bug 修复
任务难度：地狱

我需要的是能合并进项目的修复/迭代，不是泛泛的方案说明：
仓库：kestra-io/kestra（Java）。项目原本是：Event Driven Orchestration & Scheduling Platform for Mission Critical Applications。当前源码入口可从 .codespellrc, .devcontainer, .editorconfig, .gitattributes, .github, .gitignore, .gitpod.yml, .pinact.yaml, .prettierignore, AGENTS.md, CLAUDE.md, Dockerfile 进入。
Kestra 的 flow worker 在任务超时、重试和状态存储短暂不可用时，可能同时写入 SUCCESS 与 RETRYING，后续依赖任务被错误触发。请从 Java executor、storage、scheduler 和 UI/API 构造故障注入，修复状态机、幂等键和补偿；验收并发 worker、暂停恢复、权限和旧 flow。
实现以最小影响面为准，保留现有协议、错误类型和日志语义；用故障注入证明失败路径会收敛、资源会释放、重试不会放大副作用，最后跑一遍项目原有测试集。
