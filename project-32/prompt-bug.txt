任务类型：Bug 修复
任务难度：困难
语言/框架：Java

请在已有的 kestra-io/kestra 项目中排查并修复一个影响生产使用的跨模块缺陷。项目定位为：Event Driven Orchestration & Scheduling Platform for Mission Critical Applications。围绕真实入口（.codespellrc,.devcontainer .editorconfig,.gitattributes .github,.gitignore .gitpod.yml,.pinact.yaml .prettierignore,AGENTS.md CLAUDE.md,Dockerfile Dockerfile.base,Dockerfile.pr LICENSE,Makefile README.md,SECURITY.md build-and-start-e2e-tests.sh,build.gradle charts,cli codecov.yml,core dev-tools,docker-compose-ci.yml docker-compose-dind.yml,docker-compose.yml docker,docs executor,gradle.properties gradle,gradlew gradlew.bat,indexer jdbc-h2,jdbc-mysql jdbc-postgres,jdbc jmh-benchmarks,lombok.config model,owasp-dependency-suppressions.xml platform,plugins processor,queue-jdbc queue,repository-mem）对应的核心链路，复现一个状态不一致、错误传播、并发边界、资源生命周期或兼容性问题；先记录触发步骤和根因，再做最小范围修复，不能绕过逻辑、删除测试或改变无关 API。补充覆盖正常、失败、边界和回归场景的测试，并验证错误信息、退出码或响应语义仍可诊断。
