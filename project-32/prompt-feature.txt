任务类型：Feature 迭代
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：kestra-io/kestra（Java）。项目原本是：Event Driven Orchestration & Scheduling Platform for Mission Critical Applications。当前源码入口可从 .codespellrc, .devcontainer, .editorconfig, .gitattributes, .github, .gitignore, .gitpod.yml, .pinact.yaml, .prettierignore, AGENTS.md, CLAUDE.md, Dockerfile 进入。
给 Kestra 增加可版本化的 flow 部署与回滚能力，支持 schema 校验、分批启用、运行中任务策略和审计。需要协调 repository、scheduler、executor、storage 与 UI，处理并发发布、依赖兼容、secret 轮换、失败回滚、重复触发和旧 flow 迁移，补齐集成/性能测试。
先验证现有行为再扩展，新增能力要有降级和迁移方案。验收不仅看成功结果，还要检查权限、取消、恢复、性能和观测信息；把关键取舍写在变更说明里，确保后续维护者能复现问题和回滚。
