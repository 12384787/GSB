任务类型：Feature 迭代
任务难度：地狱

这个问题在真实运行里很难抓，但我希望你把它当线上故障处理：
仓库：firecrawl/firecrawl（多语言）。项目原本是：。当前源码入口可从 .gitattributes, .github, .gitignore, .gitmodules, AGENTS.md, CLAUDE.md, CONTRIBUTING.md, LICENSE, README.md, SELF_HOST.md, apps, docker-compose.yaml 进入。
给 Firecrawl 增加可恢复的站点级抓取会话，支持 sitemap/链接发现、增量去重、暂停继续、代理轮换和结构化抽取。需要贯通 API、队列、crawler、存储和 webhook，处理 robots/速率限制、部分失败、版本化结果、幂等回调和旧接口，补齐端到端、压测和安全测试。
先验证现有行为再扩展，新增能力要有降级和迁移方案。验收不仅看成功结果，还要检查权限、取消、恢复、性能和观测信息；把关键取舍写在变更说明里，确保后续维护者能复现问题和回滚。
